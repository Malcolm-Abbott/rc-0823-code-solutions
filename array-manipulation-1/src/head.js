/* exported head */

/*
PSUEDOCODE:
Define an array with the name head that takes one parameter, array
Return the first element of the array from the function
*/

function head(array) {
  return array[0];
}
