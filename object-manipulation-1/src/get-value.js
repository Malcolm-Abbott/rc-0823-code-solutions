/* exported getValue */

/*
PSEUDOCODE:
Define a function named getValue which takes two parameters -> object, key <-
return the value of the key (parameter) property of the object (parameter object)
*/
function getValue(object, key) {
  return object[key];
}
