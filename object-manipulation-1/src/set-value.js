/* exported setValue */

/*
PSEUDOCODE:
Define a function named setValue which takes three parameters: object, key, value
Assign value to the object at key index
*/
function setValue(object, key, value) {
  object[key] = value;
}
