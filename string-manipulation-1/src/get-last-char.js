/* exported getLastChar */

/*
Define a function named getLastChar that takes one parameter a string.
access and return the last index element of the string

*/
function getLastChar(string) {
  return string.at(-1);
}
